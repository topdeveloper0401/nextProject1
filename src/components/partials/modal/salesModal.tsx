import Link from "next/link";


interface ModalProps {
    href: string
    children: React.ReactNode
    className?: string,
    onClick?: () => void
}

const Index: React.FC<ModalProps> = ({ href, children, className, onClick }) => {



    return (
        <>
            
        </>
    )
}

export default Index;